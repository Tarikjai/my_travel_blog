<%- include('./layouts/header.ejs') %>


<section id="blog">
    <div class="blog-heading">
        <!--Heading-->
        <span>My Recent Post</span>
        <h3>My Blog</h3>
    </div>
    <!--blog-container-->
    <div class="blog-container">
        <% countries.forEach(function(country) { %>
        <!-- Box 1-->
        <div class="blog-box">
            <!-- img -->
            <div class="blog-img">
                <img src="<%= country.image %>" alt="blog">
            </div>

            <!-- text -->
             <div class="blog-text">
                <span><%= country.createdAt.toLocaleDateString(); %></span>
                <a href="#" class="blog-title"><%= country.name %></a>
                <p><%= country.description %></p>
                <a href="/country/<%= country.id %>">Read More</a>
            </div>
        </div>
        <% }); %>
       
    </div>
</section>

<%- include('./layouts/footer.ejs') %>



<!--
<body class="flex flex-col min-h-screen">

    <section class="flex-grow">
        <div class="card-container max-w-7xl mx-auto p-4">
            <h1 class="my-4 text-2xl font-bold">Liste des pays</h1>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <% countries.forEach(function(country) { %>
                    <div class="card bg-white shadow-md rounded-lg overflow-hidden">
                        <img src="<%= country.image %>" class="w-full h-48 object-cover" alt="<%= country.name %>">
                        <div class="card-body p-4">
                            <h5 class="card-title text-xl font-semibold"><%= country.name %></h5>
                            <h6 class="card-subtitle mb-2 text-gray-500">Capitale : <%= country.capital %></h6>
                            <p class="card-text text-gray-700"><%= country.description %></p>
                            <div class="flex justify-between mt-4">
                                <a href="/country/<%= country.id %>" class="bg-blue-700 text-white px-4 py-2 rounded hover:bg-blue-700 ">En savoir plus</a>

                                <form action="/api/countries/<%= country.id%>?_method=DELETE" method="POST" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600" >
                                    <button type="submit">Delete</button>
                                </form>
                                <a href="/edit/<%= country.id %>" class="bg-gray-300 text-gray-800 px-4 py-2 rounded hover:bg-gray-400">Edit</a> -- Bouton Edit ajouté ici 
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
</section>

<script src="/script/script.js"></script>
</body>

</html>
-->